import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:selection_menu/components_configurations.dart';

void main() {
  group("ProjectGroupDropdownMenu", () {
    // final animationComponentData = AnimationComponentData(
    //   constraints: const BoxConstraints(maxHeight: 10.0),
    // );

    testWidgets(
      "applies the maxHeight to the container's constraints from the AnimationComponentData's constraints",
      (tester) async {},
    );

    testWidgets(
      "contains the AnimationComponentData's child widget",
      (tester) async {},
    );
  });
}
