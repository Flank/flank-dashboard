import 'package:flutter_test/flutter_test.dart';

void main() {
  group("ProjectGroupDropdownItem", () {
    testWidgets(
      "throws an AssertionError if the projectGroupDropdownItemViewModel is null",
      (tester) async {},
    );

    testWidgets(
      "displays the name of the given projectGroupDropdownItemViewModel",
      (tester) async {},
    );

    testWidgets(
      "applies a text style from the project group dropdown item theme to the text",
      (tester) async {},
    );

    testWidgets(
      "applies the hover color from the project group dropdown item theme if the widget is hovered",
      (tester) async {},
    );

    testWidgets(
      "applies the background color from the project group dropdown item theme if widget is not hovered",
      (tester) async {},
    );
  });
}
