import 'package:flutter_test/flutter_test.dart';

void main() {
  group("ProjectGroupsDropdown", () {
    testWidgets(
      "contains the dropdown menu widget",
      (tester) async {},
    );

    testWidgets(
      "applies an animation duration constant to the menuAnimationDuration parameter of the dropdown menu widget",
      (tester) async {},
    );

    testWidgets(
      "delegates a list of project group dropdown item view models to the dropdown menu widget",
      (tester) async {},
    );

    testWidgets(
      "selects the first project group dropdown item view model initially",
      (tester) async {},
    );

    testWidgets(
      "contains a ProjectGroupDropdownMenu widget as a menu of the dropdown menu widget",
      (tester) async {},
    );

    testWidgets(
      "displays the given items using the ProjectGroupDropdownItem widget",
      (tester) async {},
    );

    testWidgets(
      "selects a project group view model on tap on ProjectGroupDropdownItem",
      (tester) async {},
    );

    testWidgets(
      "displays a new of the selected ProjectGroupDropdownItemViewModel",
      (tester) async {},
    );

    testWidgets(
      "has a dropdown button that contains the dropdown icon",
      (tester) async {},
    );
  });
}
