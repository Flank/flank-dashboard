import 'package:flutter/material.dart';

/// The class that stores the theme data for the project group card.
class AddProjectGroupCardTheme {
  /// The main color of the add project group card theme.
  final Color primaryColor;

  /// The background color of the add project card theme.
  final Color backgroundColor;

  /// Creates the [AddProjectGroupCardTheme].
  const AddProjectGroupCardTheme({this.primaryColor, this.backgroundColor});
}
