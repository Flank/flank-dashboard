import 'package:flutter/material.dart';

/// A class that provides a style configuration for the project build status widget.
@immutable
class ProjectBuildStatusStyle {
  /// A [Color] of the background.
  final Color backgroundColor;

  /// Creates an instance of the [ProjectBuildStatusStyle].
  const ProjectBuildStatusStyle({
    this.backgroundColor,
  });
}
