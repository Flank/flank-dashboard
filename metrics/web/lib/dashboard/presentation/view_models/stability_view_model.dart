import 'package:metrics/dashboard/presentation/view_models/percent_view_model.dart';

/// A specific [PercentViewModel] for the stability widget.
class StabilityViewModel extends PercentViewModel {
  /// Creates the [StabilityViewModel] with the given [value].
  const StabilityViewModel({double value}) : super(value);
}
