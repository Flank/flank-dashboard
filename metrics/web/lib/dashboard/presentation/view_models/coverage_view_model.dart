import 'package:metrics/dashboard/presentation/view_models/percent_view_model.dart';

/// A specific [PercentViewModel] for the coverage widget.
class CoverageViewModel extends PercentViewModel {
  /// Creates the [CoverageViewModel] with the given [value].
  const CoverageViewModel({double value}) : super(value);
}
