/// The enum that represents the project group project ids codes.
enum ProjectGroupProjectIdsValidationErrorCode {
  /// Indicates that a project ids exceeds the selection limit.
  projectsSelectionLimitExceeded,
}
