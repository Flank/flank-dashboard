import 'package:flutter/cupertino.dart';

/// Represents the project group id param.
@immutable
class ProjectGroupIdParam {
  final String projectGroupId;

  /// Creates the [ProjectGroupIdParam] with the given [projectGroupId].
  const ProjectGroupIdParam(this.projectGroupId);
}