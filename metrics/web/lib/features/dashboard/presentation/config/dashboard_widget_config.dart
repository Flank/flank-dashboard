import 'package:flutter/material.dart';

/// Holds the configuration of dashboard widgets.
class DashboardWidgetConfig {
  /// The flex factor of the leading of the tile on the dashboard.
  static const int leadingFlex = 3;

  /// The flex factor of the trailing of the tile on the dashboard.
  static const int trailingFlex = 5;

  /// The padding of the content on the dashboard page.
  static const EdgeInsets contentPadding =
      EdgeInsets.symmetric(horizontal: 64.0);
}
