/// Enum that represents the error codes of an email validation.
enum EmailValidationErrorCode {
  /// Indicates that an email is null.
  isNull,

  /// Indicates that an email is malformed.
  invalidEmailFormat,
}
