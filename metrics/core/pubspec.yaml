name: metrics_core
description: A package providing a base implementations for Mestrics applications to support code reuse.

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  meta: ^1.1.8
  equatable: ^1.1.1

dev_dependencies:
  lint: ^1.1.1
  test: ^1.14.1
